<%= form_for(@plateform) do |f| %>
  <% if @plateform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plateform.errors.count, "error") %> prohibited this plateform from being saved:</h2>

      <ul>
      <% @plateform.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
