<%= form_for(@domaine) do |f| %>
  <% if @domaine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@domaine.errors.count, "error") %> prohibited this domaine from being saved:</h2>

      <ul>
      <% @domaine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
