<%= form_for(@style_interaction) do |f| %>
  <% if @style_interaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@style_interaction.errors.count, "error") %> prohibited this style_interaction from being saved:</h2>

      <ul>
      <% @style_interaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
